#include <stdio.h>
int main(){
    char i = 0;
    while(i != 25){
        printf("El numero es: %i, el char es: %c\n", i, i);
        i++;
    }
    // printf("un besito en lo prohibido");
    return 0;
}